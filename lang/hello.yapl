// hello, world.
#asm(".bootstrap");

const byte hello[] = "hello, world.\n";

func void main() {
    Putstr(&hello);
}

func void Putc(byte b) {
    // no check for overrun in this simpleminded function.
    #asm("ldi r2 96");
    #asm("ssp r1 r2");
}

func void Putstr(@byte bp) {
    while (@bp != 0) {
	Putc(@bp);
	bp = bp + 1;
    }
}
