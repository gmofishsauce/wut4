// Regression test for C1: STR vs STRLIT keyword mismatch.
// yparse emits "STR ..." for string literals; reader.go looks for "STRLIT".
// With the bug the literal is silently dropped and the return becomes void,
// causing a "non-void function must return a value" error.
func @byte greeting() {
    return "hello";
}
